# Server Configuration (single server for both OAuth + MCP)
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_BASE_URL=http://localhost:8000

# JWT Configuration
JWT_ALGORITHM=RS256
JWT_ISSUER=http://localhost:8000
JWT_AUDIENCE=mcp-greeting-server
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Key Storage
PRIVATE_KEY_PATH=./keys/private_key.pem
PUBLIC_KEY_PATH=./keys/public_key.pem

# Client Storage
CLIENT_STORAGE_PATH=./oauth/clients.json
